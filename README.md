# AI Image Editor
## Run locally
npm install
npm run dev

## Deploy to Vercel
Push this folder to GitHub, import the repository in Vercel, and deploy. No environment variables are required for the browser-based features.

## AI integration
Relighting and AI upscale buttons are intentionally API-ready placeholders. Connect a provider through server-side routes and keep API keys in Vercel environment variables.
